apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: kafka
spec:
  type: ClusterIP       # ✅ 내부 클러스터 통신만 가능
  selector:
    app: kafka
  ports:
  - name: kafka
    port: 9092
    targetPort: 9092
